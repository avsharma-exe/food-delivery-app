import { TopRatedRestaurantPipe } from './top-rated-restaurant.pipe';

describe('TopRatedRestaurantPipe', () => {
  it('create an instance', () => {
    const pipe = new TopRatedRestaurantPipe();
    expect(pipe).toBeTruthy();
  });
});
